apiVersion: v1
kind: Service
metadata:
  name: shutdown-simulator
  labels:
    app: shutdown-simulator
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: shutdown-simulator
  type: ClusterIP
